## moreHistoryThanMathAbsences Method
I use a traditional for loop to loop through each student in the history list
For each student in that list to see if they are ALSO in the math list
If both conditions were met then we check if their history absences is greater than the math absences
If so then a previously set variable "num" is increased by 1
Then we return num
